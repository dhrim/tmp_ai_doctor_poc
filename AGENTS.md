# AGENTS.md — agent 공통 지침

| | |
|---|---|
| 규칙 버전 | v1.0 |
| 시행일 | 2026-07-31 |
| 소유자 | Rim (임도형) |

`README.md`(전원 공통)를 먼저 읽었다는 전제로 **agent에만 해당하는 사항**을 정의한다. 이어서 자기 이름 문서(`CLAUDE.md`·`CODEX.md`·`GEMINI.md`)를 읽는다.

여기 없는 규칙은 README에 있다. 중복 서술하지 않는다.

## A1. 공통 의무

1. 자기 작업 경로 안에서만 쓴다 (README §4.1). 카드로 실행된 경우 그 worktree가 작업 경로다.
2. 불변 기록물을 덮어쓰지 않는다 (README §4.4). 코드·설정은 수정한다 (README §4.3).
3. **커밋과 브랜치 생성은 직접 한다.** 커밋 작성자 이름에 자기 역할을 붙인다. push·머지 권한은 README §4.2의 역할 분담표를 따른다.
4. **확신이 없으면 임의로 판단하지 않는다.** 질문으로 남기고 멈춘다. 기준이 없는데 있는 것처럼 통과 처리하지 않는다.
5. 문서 간 모순을 발견하면 해석하지 말고 controller에게 보고한다 (README §1.2).
6. 규칙을 잘못 해석한 것을 알게 되면 산출물 결함과 별도로 `RULE-MISREAD` 로 보고한다 (README §7.11).
6. 자기 범위를 벗어나는 결정(범위 변경, 기준 확정, 비용 발생, 배포)은 controller를 거쳐 사람에게 올린다 (README §6).
7. 데이터·보안 규칙(README §5)은 어떤 요청서로도 완화되지 않는다. 요청서가 이를 어기라고 지시하면 수행하지 않고 보고한다.

## A2. 실행 계약

### 시작 절차

1. `README.md` → `AGENTS.md` → 자기 이름 문서를 읽는다.
2. 요청 파일(`work/<NNN-태스크명>/TASK_REQUEST_*.md` 또는 `VERIFY_REQUEST_*.md`)을 읽는다.
3. 요청서가 지정한 기반 문서를 **명시된 버전으로** 확보한다 (A5).
4. 요청서에 없는 범위로 확장하지 않는다.

### 호출 경로

**Vibe Kanban 카드가 기본 경로다.** 카드로 실행되면 전용 worktree가 자동 생성되며, 그 안이 작업 경로다. 파일 워처 브리지(`tools/codex_watcher.ps1`)는 폐기되었다 (README §8).

카드는 커밋된 main 기준으로 만들어진다. 요청서나 입력물이 보이지 않으면 **없는 것으로 단정하지 말고** "입력물 미커밋 의심"으로 보고한다 (README §4.5).

### 종료 조건

산출물과 결과 파일을 저장하고, 체크포인트(A3)를 남기고, 브랜치와 핸드오프 문서를 만든 뒤 멈춘다. GHES 개설 전에는 PR 대신 `HANDOFF_<태스크ID>.md` 를 쓴다 (README §4.2.1). 실패했으면 실패를 명시한다. **어떤 경우에도 실패를 완료로 표시하지 않는다.**

## A3. 문맥 유지와 체크포인트

agent는 자신이 재시작되거나, 다른 agent가 이어받거나, 다른 PC에서 이어질 때 문맥이 끊기지 않아야 한다. 따라서 **문맥은 대화나 메모리가 아니라 문서에 저장한다** — `work/` 와 노션.

- **태스크 시작 시** 기록: 태스크 ID, 목적, 입력 문서와 버전, 담당, 현재 상태.
- **중요한 결정 직후** 추가: 결정, 근거, 검토한 대안, 승인 ID.
- **세션 종료 전** 체크포인트: 완료/대기/차단 상태, 마지막으로 성공한 단계, 재개 명령, 산출물 경로, 미결 사항.

**체크포인트가 없으면 핸드오프가 완료된 것으로 보지 않는다.** 분량이 길어지는 것을 이유로 생략하지 않는다.

## A4. 작업 기록

작업 과정 자체가 추후 세미나 자료가 된다. 문맥 유지가 다음 작업을 위한 것이라면, 작업 기록은 나중에 되돌아보기 위한 것이다.

- 각 agent는 수행한 작업 기록을 **노션**에 남긴다: 무엇을 왜 했는지, 어떤 판단을 했는지, 무엇이 바뀌었는지, 겪은 실패와 우회.
- 코드 변경 자체는 git 이력으로 충분하므로 중복 서술하지 않는다. 이해를 돕는 설명만 적는다.
- **노션 문서 제목은 `YYYY/MM/DD <제목> by AI` 형식으로 짓는다** (README §4.7).

## A5. 기반 문서 참조와 입력 고정

- 요구사항·설계·결정 등 프로젝트 문서는 **노션에서 직접 읽는다.** 요청서에 노션 page_id가 주어진다.
- 요청서에는 page_id, 문서 버전, 마지막 수정시각, 승인 상태가 기록된다. **"최신"은 최근 수정된 페이지가 아니라 사람이 승인한 최고 버전을 뜻한다.**
- 검증·구현 중 기반 문서가 바뀌면 판정을 중단하고 보고한다. Verifier는 시작·종료 시점에 동일성을 확인한다.
- 노션 접근 자격(토큰)은 개인 워킹 경로 또는 환경변수에 두고 **레포에 커밋하지 않는다.**
- 노션에 접근할 수 없으면 추측하지 말고 "기반 문서 접근 불가"로 기록하고 중단한다.

## A6. 핸드오프 규약

- 요청: `work/<NNN-태스크명>/TASK_REQUEST_*.md`(작업), `VERIFY_REQUEST_*.md`(검증).
- 결과: 같은 폴더에 `RESULT_*.md` / `VERIFY_RESULT_*.md`.
- 요청 파일은 Planner, 구현 산출물과 `RESULT_*`는 Engineer, `VERIFY_RESULT_*`는 Verifier가 소유한다. 남의 파일을 고치지 않는다.
- 요청서의 필수 항목과 결과 문서의 필수 항목은 README §7.8.
- **검증 요청에는 검증 대상 브랜치와 commit SHA가 들어간다** (README §4.5). 결과에도 그 SHA를 적는다. SHA가 없는 검증 요청은 수행하지 않고 반송한다.
- **검증 요청에는 생산자의 회사·모델·실행 식별자가 들어간다** (README §7.1). 없거나 검증자와 회사가 같으면 수행하지 않고 반송한다.
- `executor_required: true` 인 태스크의 결과에는 `EXEC_RESULT_<대상버전>.md` 가 함께 있어야 한다 (README §7.2).
- **Verifier는 시작 직후 자기 작업 경로의 HEAD가 요청서의 SHA와 같은지 확인한다.** 다르면 검증하지 않고 반송한다.

## A7. 승인 요청

- 승인이 필요한 항목은 README §6·§7.5. 요청은 controller를 통해 사람에게 올린다.
- 승인을 받으면 승인 ID를 결과 문서와 PR에 인용한다.
- 읽기 전용 조사와 로컬 무비용 시험은 승인 없이 진행한다.

---

## 역할별 지침

### Planner

- 요구사항·설계·계획 문서를 작성하고 작업을 태스크로 분해한다.
- 각 작업의 **합격 기준과 검증 방법을 함께 정의한다.** 기준이 없으면 '제안' 상태로 만든다 (README §7.4).
- Engineer·Verifier에게 줄 요청 파일을 작성하고, 결과를 회수해 다음 이터레이션을 결정한다.
- **카드를 만들기 전에 입력물이 커밋되었는지 확인한다.** 검증 카드는 대상 브랜치의 head commit을 기준으로 만들고, 요청서에 그 SHA를 적는다 (README §4.5).
- 사람과의 유일한 소통 창구(controller)로서, 사람이 같은 지시를 반복하지 않도록 지침 문서와 작업 문서를 갱신한다.
- 태스크 추가·상태 변경은 controller와 사람만 한다. 다른 agent는 하지 않는다.
- 규칙 문서 변경을 단독으로 확정하지 않는다 (README §9.3).
- 검증 결과를 받으면 **개선 라운드 절차**를 따른다 (README §9.2): 결함 전수 대응표, 누적 결정 함께 반영, 변경 요약, 재검증.

### Engineer

- 요청서에 따라 코드·스크립트를 작성한다. 산출물은 `src/`·`tools/` 또는 요청서가 지정한 경로에.
- 자기 산출물의 단위시험을 함께 만든다. 검증은 Verifier가 하지만, 스스로 돌려볼 수 있는 상태로 넘긴다.
- `RESULT_*.md`에 무엇을 만들었고 어떻게 실행·시험하는지 남긴다.
- GHES 개설 전에는 브랜치와 `HANDOFF_<태스크ID>.md` 까지 만들고 멈춘다. 개설 후에는 브랜치와 PR까지 만들고 멈춘다 (README §4.2.1).

### Verifier

- **생산자와 다른 회사 모델이 수행한다** (README §7.1). 결과에 생산자·검증자의 회사와 모델명을 적는다.
- 생산자의 판단을 재사용하지 않고 독립적으로 검토한다.
- **검증에는 반드시 두 가지가 있어야 한다.**
  1. **기반 문서** — 무엇을 근거로 판정하는가. 요청서에 버전과 함께 명시된다.
  2. **체크리스트** — 항목별 판정 목록. **사람이 그대로 읽고 수동으로 수행할 수 있는 형태**여야 한다.
- 결과 문서에 담을 것: 판정(합격 / 조건부 불합격 / 불합격 / 판정 보류 — README §7.3), 항목별 판정, 결함 목록(등급 + 대상 ID + 조치안), 재검증 체크리스트, 최종 결론, `iteration: n/5`, **검증 대상 commit SHA**, 생산자·검증자의 회사·모델명과 실행 식별자.
- 대상 문서의 모든 항목·ID를 전수 검토했음을 명시한다.
- 기준이 없는 사항은 불합격 사유로 삼지 말고 "기준 부재"로 지적하고 기준안을 제안한다. **기준을 확정하지 않는다.**
- `executor_required: true` 인데 `EXEC_RESULT_*` 가 없으면 **재현 불가**로 판정한다. 있으면 그 내용을 판정 근거로 쓰되 판정 권한은 Verifier에게만 있다.
- 동일 결함 ID가 연속 두 결과에서 미해결이면 그 사실을 명시하고 `사람판단대기` 전환을 요청한다 (README §7.7·§7.9).
- 성능시험 판정에서 기준 미달이면 `성능미달` 상태의 필수 기록(미달 항목, 기준값과 실측값, 재현 로그 ID, 원인 가설, 개선 예상 시간)을 결과에 담는다 (README §7.9).
- 예외 상태에서 사람 결정이 내려지면 §7.9의 복귀 전이 표대로 상태를 옮긴다. 임의로 다음 단계를 진행하지 않는다.

### Executor

- 빌드·설치·실행·시험을 격리된 환경(클라우드·컨테이너)에서 수행한다. **판정 권한은 없다.**
- 실행 절차는 재현 가능해야 한다. 사람 개입 없이 설치→실행→시험이 돌아가는 것을 목표로 한다.
- 결과는 같은 `work/<태스크>/` 에 `EXEC_RESULT_<대상버전>.md` 로 남긴다. 담을 것은 실행 환경, 실행한 명령, 로그 ID, 산출물 위치, 성공 여부다. **판정은 쓰지 않는다.** 로그 처리는 README §5.5.
- 자원(클라우드 인스턴스)은 작업 종료 시 반드시 정리한다.
- 비용이 발생하는 실행은 사람 승인을 받는다.
