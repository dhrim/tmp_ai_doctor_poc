# HUMAN.md — 사람에게만 해당하는 사항

| | |
|---|---|
| 규칙 버전 | v1.0 |
| 시행일 | 2026-07-31 |

`README.md`(전원 공통)를 읽은 뒤 이 문서를 읽는다. agent용 지침(`AGENTS.md` 등)은 참고로만 본다.

사람만 할 수 있는 일의 **목록**은 README §6에 있다. 이 문서는 그것을 **어떻게 수행하는지** 적는다.

## 승인하는 법

1. agent의 승인 요청은 controller를 통해 온다. 요청에는 대상, 근거, 위험, 대안이 들어 있다.
2. 승인하면 노션 결정 로그에 **승인 ID**를 부여해 기록한다: 승인 ID, 날짜, 대상, 승인자, 유효기간.
3. agent는 그 ID를 결과 문서와 PR에 인용한다. **ID 없는 승인은 없던 것으로 취급된다.**
4. 잠정 기준을 확정할 때도 같은 방식으로 기록한다 (README §7.4).

## 리뷰하고 머지하는 법

- PR의 diff와 함께 그 작업의 요청서(`work/<태스크>/…REQUEST…`)와 결과(`…RESULT…`)를 같이 본다. 무엇을 근거로 했는지가 거기 있다.
- 완료 정의(README §7.6)를 충족하지 않으면 통합하지 않는다. **조건부 불합격은 완료가 아니다.**
- GHES 개설 전에는 PR이 없다. `work/<태스크>/HANDOFF_*.md` 와 `git diff main..<브랜치>` 로 검토하고 직접 통합한다 (README §4.2.1).
- **HANDOFF가 유일한 감사 증거다.** 리뷰어·일시·검토 명령·검증 합격 SHA·통합 대상 SHA를 확인하고, **통합 후 통합 결과 commit을 HANDOFF에 적는다.** 이 기록이 없으면 완료가 아니다.
- **검증이 합격한 commit SHA를 포함한 브랜치만 통합한다.** 검증 후 commit이 바뀌었으면 합격은 무효다 (README §4.5).
- 검증 결과의 체크리스트는 사람이 직접 수행할 수 있는 형태로 작성된다. 필요하면 agent 없이 그 체크리스트로 수동 검증해도 된다.
- 예외 통합은 **경미 결함 또는 비필수 기준의 유예에만** 허용된다. 중대 결함·보안·환자정보 위험·핵심 기능 실패·조건부 불합격·불합격은 위험수용 승인으로도 통합할 수 없다 (README §7.6).

## agent에게 일을 시키는 법

- **agent에게 직접 지시하지 않는다.** 지시는 controller agent에게만 하고, controller가 지침 문서와 작업 요청으로 다른 agent에 전달한다. 같은 지시를 두 번 하지 않게 하는 것이 controller의 책임이다.
- 작업 방식에 대한 지시가 바뀌면 controller가 규칙 문서 변경을 제안한다. **구두로만 전달된 규칙은 유지되지 않는다.**
- 규칙 문서 변경은 다른 회사 모델의 검증을 거쳐 사람이 승인·머지한다 (README §9).
- **카드를 만들기 전에 입력물이 커밋되어 있어야 한다** (README §4.5). 미커밋 상태로 카드를 돌리면 agent는 구버전을 보고 일한다.

## 비상시

보안 사고, 환자정보 노출 의심, 비용 급증, 서비스 장애, controller 접근 불가 — 이때는 controller를 거치지 않고 **실행 중인 agent를 직접 즉시 중지시킨다.** 이후 README §5.1의 사고 절차를 따른다.

controller 접근 불가가 1세션 이상 이어지면 임시 controller를 승인 ID로 지정한다 (README §11.2).

## 예외 상황에서 결정하기

agent가 `사람판단대기`·`환경차단`·`성능미달`·`결정대기` 로 올린 건은 README §7.9의 결정 항목 중 하나를 골라 승인 ID로 남긴다. 고르지 않으면 그 태스크는 멈춘 채로 있는다.

**2영업일 이상 부재가 예상되면** 미리 대리 결정자·권한 범위·유효기간을 승인 ID로 지정한다. 지정하지 않으면 비용·보안·범위·성능 기준 관련 작업이 모두 멈춘다.

## 새로 합류했다면

1. `README.md`를 읽는다. 프로젝트 전제와 규칙이 거기 있다.
2. 레포를 자기 경로에 clone 한다. main은 직접 편집하지 않는다 — 브랜치에서 작업하고 PR로 올린다.
3. 자기 태스크와 배경은 노션과 태스크 관리 도구에서 확인한다 (정본 도구는 README §10 확인).
4. 규칙·규약·핸드오프의 정본은 레포와 노션에 있다. 단, 자격증명·MCP 등록·도구 설치·캐시·마지막 확인 시각 같은 **장비별 실행 설정은 개인 워킹 경로나 OS 자격 저장소에 두어도 된다.** 그 설정 절차는 비밀값 없이 문서화한다.
