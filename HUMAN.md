# HUMAN.md — 사람에게만 해당하는 사항

`README.md`(전원 공통)를 읽은 뒤 이 문서를 읽는다. agent용 지침(`AGENTS.md` 등)은 참고로만 본다.

## 사람만 할 수 있는 일

agent는 아래를 하지 않는다. 반드시 사람이 판단·수행한다.

- **git 머지** (agent는 PR 생성까지)
- **미결 사항 결정** — 기능 범위, 대상 질환, 우선순위 등
- **잠정 기준의 확정** — 시험 통과값처럼 측정 후 확정해야 하는 수치
- **비용 발생·클라우드 배포 승인**
- **이터레이션 중단 판단** — 검증이 같은 쟁점으로 반복될 때
- **대용량 파일의 저장 위치 판단** — agent가 물어오면 결정
- **태스크 추가·수정·삭제** (agent 중에는 controller만 가능)
- 계정·자격증명 입력, 외부 기관 대응, 대외 문서 제출

## 새로 합류했다면

1. `README.md`를 읽는다. 프로젝트 전제와 규칙이 거기 있다.
2. 레포를 자기 경로에 clone 한다. 정본(main)은 직접 편집하지 않는다 — 브랜치에서 작업하고 PR로 올린다.
3. 자기 태스크와 배경은 노션과 태스크 관리 도구에서 확인한다.
4. 개인 PC에 별도 설정을 만들지 않는다. 지침·규약·핸드오프는 모두 레포에 있다.

## agent와 일하는 법

- **agent에게 직접 지시하지 않는다.** 사람의 지시는 controller agent에게만 하고, controller가 지침 문서와 작업 요청으로 다른 agent에 전달한다. 같은 지시를 사람이 두 번 하지 않게 하는 것이 controller의 책임이다.
- 작업 방식에 대한 지시가 바뀌면 controller가 규칙 문서를 갱신한다. 구두로만 전달된 규칙은 유지되지 않는다.
- 검증 결과의 체크리스트는 사람이 직접 수행할 수 있는 형태로 작성된다. 필요하면 agent 없이 그 체크리스트로 수동 검증해도 된다.

## 리뷰할 때

- PR의 diff와 함께, 그 작업의 요청서(`work/<태스크>/…REQUEST…`)와 결과(`…RESULT…`)를 같이 본다. 무엇을 근거로 했는지가 거기 있다.
- 검증을 통과하지 않은 작업은 완료로 보지 않는다.
