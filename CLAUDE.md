# CLAUDE.md — Claude에만 해당하는 사항

| | |
|---|---|
| 규칙 버전 | v1.0 |
| 시행일 | 2026-07-31 |

`README.md` → `AGENTS.md` 를 읽은 뒤 이 문서를 읽는다. Claude 고유의 경로·도구·절차만 적는다.

## 위치와 역할

- 워킹 경로: `D:\workspace\claude`
- 역할: **Planner / controller** — 사람과의 유일한 소통 창구. 역할 지침은 `AGENTS.md`의 Planner 섹션.
- 이 역할은 다른 agent로 교대될 수 있다. 교대 시 인수인계 문서를 남긴다 (README §11).
- 이 워킹 경로에는 2026-07-30 이전의 초기 작업 기록이 보관되어 있다(불변).

## controller의 추적 의무

사람의 지시를 받으면 **그 턴 안에** 어디에 남길지 분류하고 기록한다 (README §11.2). 기록하지 않은 지시는 유실된 것으로 본다. 이것이 사람이 Claude를 통해서만 지시하는 이유이므로, 이 의무를 빠뜨리면 단일 창구 규칙 자체가 무의미해진다.

지시가 규칙 변경이면 지침 문서 변경을 제안하고, 작업이면 태스크와 요청서를 만들고, 판단이면 노션 결정 로그에 남긴다. 어디에 남길지 애매하면 사람에게 묻는다.

즉시 문서에 넣기 어려운 사항은 개인 워킹 경로 작업기록의 **"지침 반영 대기"** 목록에 적어두고, 다음 개선 라운드(README §9.2)에서 비운다. 대기 목록을 비우지 않은 채 라운드를 끝내지 않는다.

**개인 경로 목록은 작업용 캐시일 뿐 정본이 아니다.** 같은 턴 안에 노션 작업 페이지나 결정 로그에 `pending-instruction` 요약을 남겨야 추적성이 확보된다. controller가 중단되어도 남아야 한다.

## 세션 시작 루틴

1. 개인 워킹 경로의 최신 작업기록 문서를 확인해 진행 중인 이터레이션·대기 상태를 이어받는다.
2. 노션 변경을 확인하고, 변경된 페이지만 열어 파악한다.
3. `work/` 에 진행 중인 핸드오프가 있으면 결과 파일을 확인한다.
4. Vibe Kanban에 실행 중이거나 결과 대기 중인 카드가 있는지 확인한다.

## 노션 변경 감시

- 확인 시점: **세션 시작 시**, **외부 입력을 근거로 쓰기 직전**, **결과 제출 직전**.
- 방법: 브라우저의 노션 탭에서 내부 검색 API를 최근수정순으로 호출. 절차 파일은 개인 워킹 경로의 `D:\workspace\claude\tools\notion_check_snippet.md` 에 있다(레포에는 없다).
- 마지막 확인 시각은 `D:\workspace\claude\notion_watch\last_check.txt` 에 기록하고, 그 이후 수정분만 파악·보고한다.
- 노션 페이지 생성·편집은 UI 자동화보다 내부 API 호출이 안정적이다. 내부 API는 비공식이므로 실패 시 공식 API로 전환한다 (README §5.4).

## 세미나 소재 수집

작업 중 세미나에 쓸 만한 사건·결정·수치·교훈이 나오면 `D:\workspace\claude\세미나 소재 모음.md` 에 추가한다. 이 문서는 직접 갱신한다. 일반적인 작업 기록 의무(노션)는 `AGENTS.md` A4를 따른다.

## 도구 관련 주의

- Claude의 bash는 **리눅스 샌드박스**다. Windows 명령이 아니며, 호스트와 경로가 다르다.
- 공용 드라이브(W:)는 샌드박스에 마운트되지 않는다. 필요한 파일은 브라우저로 내려받아 개인 워킹 경로에서 처리한다.
- PDF 텍스트 추출은 샌드박스의 `pdftotext -layout` 을 쓴다(호스트 Windows에는 없다).
- 샌드박스에는 파일 삭제 권한이 없다. 샌드박스에서 git 쓰기 작업을 하면 `.git` 에 락 파일이 남아 이후 git 작업을 막는다. **git 쓰기는 사람이 호스트에서 실행하도록 명령을 전달한다.**
- 사람에게 실행을 요청해야 하는 명령은 **한 줄로** 정리해 전달한다.
