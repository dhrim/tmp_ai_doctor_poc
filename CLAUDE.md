# CLAUDE.md — Claude에만 해당하는 사항

`README.md` → `AGENTS.md` 를 읽은 뒤 이 문서를 읽는다. 여기에는 Claude 고유의 사항만 적는다.

## 위치와 역할

- 워킹 경로: `D:\workspace\claude`
- 현재 역할: **Planner / controller** (사람과의 유일한 소통 창구). 역할 지침은 `AGENTS.md`의 Planner 섹션. 이 역할은 추후 다른 agent로 교대될 수 있다.
- 이 워킹 경로에는 2026-07-30 이전의 초기 작업 기록도 보관되어 있다(불변).

## 세션 시작 루틴

1. 최신 작업기록 문서를 확인해 진행 중인 이터레이션·대기 상태를 이어받는다.
2. 노션 변경을 확인하고, 변경된 페이지만 열어 파악한다.
3. 진행 중인 핸드오프가 있으면 `work/` 의 결과 파일을 확인한다.

## 노션 변경 감시

- 세션 시작 시와 작업 중 수시로 확인한다. 방법: `tools/notion_check_snippet.md` (브라우저의 노션 탭에서 내부 검색 API를 최근수정순으로 호출).
- 마지막 확인 시각은 개인 워킹 경로에 기록하고, 그 이후 수정분만 파악·보고한다.
- 노션 페이지 생성·편집은 UI 자동화보다 내부 API 호출이 안정적이다.

## 세미나 소재 수집

- 작업 중 세미나에 쓸 만한 사건·결정·수치·교훈이 나오면 개인 워킹 경로의 `세미나 소재 모음` 문서에 수시로 추가한다. (이 문서는 직접 갱신 허용)
- 일반적인 작업 기록 의무(노션)는 `AGENTS.md`를 따른다. 이것은 그 위에 추가되는 별도 수집이다.

## 도구 관련 주의

- 리눅스 샌드박스(bash)는 네트워크·마운트에 제약이 있다. 공용 드라이브(W:)는 마운트되지 않으므로 필요한 파일은 브라우저로 내려받아 개인 워킹 경로에서 처리한다.
- PDF 텍스트 추출은 샌드박스의 `pdftotext -layout` 을 쓴다.
- 사람에게 실행을 요청해야 하는 명령은 한 줄로 정리해 전달한다.
