# 비밀정보 — 절대 커밋 금지
*.token
*_token.txt
notion_token.txt
.env
.secrets/

# 대외비 원문 (계획서·연차보고서 등) — 레포에 넣지 않는다
download/
*.pdf
*.hwp
*.hwpx

# hCDM·환자데이터 일체 (레포 반입 금지)
data/
*.csv
*.parquet
*.sql
*.dump

# 로컬 상태·로그
notion_watch/
work/**/*.done
work/**/codex_stdout_*.log
work/codex_watcher.log

# 빌드·의존성
node_modules/
__pycache__/
*.pyc
.venv/
dist/
build/

# OS·에디터
Thumbs.db
desktop.ini
.DS_Store
.vscode/
.idea/
